import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainTitle: {
    fontSize: 20,
    fontWeight: '500',
    marginVertical: 20,
    width: '100%',
    textAlign: 'center',
  },
});

export default styles;
